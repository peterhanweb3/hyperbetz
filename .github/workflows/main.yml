name: 🚀 Auto Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2222 
          script: |
            cd /var/www/html/hyperbetz/
            git pull origin main
            npx prisma generate
            npm i
            npm run build
            pm2 restart web_hyperbetz
